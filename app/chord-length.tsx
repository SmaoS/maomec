import { Share, ScrollView, Text } from 'react-native';
import { useState } from 'react';
import { Buttons, CalculatorCard, FormulaCard, NumericInput, ResultCard, Screen, SectionHeader, styles } from '../src/components';
import { calculateChordLength, roundForDisplay } from '../src/domain/math';
import { saveHistory } from '../src/storage/preferences';
import { parseLocalizedNumber } from '../src/utils/input';
export default function Chord(){const[n,setN]=useState('');const[d,setD]=useState('');const[result,setResult]=useState<ReturnType<typeof calculateChordLength>|null>(null);const[error,setError]=useState('');const calculate=()=>{try{const holes=parseLocalizedNumber(n),diameter=parseLocalizedNumber(d);if(holes===null||diameter===null)throw new Error('Completa los datos para calcular.');const r=calculateChordLength(holes,diameter);setResult(r);setError('');void saveHistory({type:'Longitud de cuerda',summary:`${holes} agujeros · Ø${diameter} mm`,result:`${roundForDisplay(r.chord)} mm`});}catch(e){setError(e instanceof Error?e.message:'Datos inválidos');setResult(null)}};const text=result?`MaoMec\n\nLongitud de cuerda\nDiámetro: ${d} mm\nAgujeros: ${n}\nSeparación angular: ${roundForDisplay(result.angle)}°\nLongitud: ${roundForDisplay(result.chord)} mm`:'';return <Screen><ScrollView><SectionHeader title="Longitud de cuerda" subtitle="Distancia recta entre perforaciones consecutivas."/><CalculatorCard><NumericInput label="Cantidad de agujeros" value={n} onChangeText={setN}/><NumericInput label="Diámetro del círculo" value={d} onChangeText={setD} unit="mm"/>{error&&<Text style={styles.error}>{error}</Text>}<Buttons onCalculate={calculate} onClear={()=>{setN('');setD('');setResult(null);setError('')}}/><FormulaCard formula="C = D × sin(180° / N)"><Text>C: cuerda · D: diámetro · N: agujeros</Text></FormulaCard></CalculatorCard>{result&&<><ResultCard label="Longitud de cuerda" value={`${roundForDisplay(result.chord)}`} unit="mm" details={<><Text style={styles.resultDetails}>Radio: {roundForDisplay(result.radius)} mm</Text><Text style={styles.resultDetails}>Separación: {roundForDisplay(result.angle)}°</Text></>}/><Text style={styles.link} onPress={()=>void Share.share({message:text})}>Compartir resultado</Text></>}</ScrollView></Screen>}
