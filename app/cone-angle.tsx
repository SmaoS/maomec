import { useState } from 'react';
import { ScrollView, Text } from 'react-native';
import { Buttons, CalculatorCard, FormulaCard, NumericInput, ResultCard, Screen, SectionHeader, styles } from '../src/components';
import { calculateConeAngle, roundForDisplay } from '../src/domain/math';
import { saveHistory } from '../src/storage/preferences';
import { parseLocalizedNumber } from '../src/utils/input';
export default function Cone(){const[D,setD]=useState(''),[d,setd]=useState(''),[l,setL]=useState('');const[r,setR]=useState<ReturnType<typeof calculateConeAngle>|null>(null);const[error,setError]=useState('');const calc=()=>{try{const values=[D,d,l].map(parseLocalizedNumber);if(values.some(v=>v===null))throw new Error('Completa los datos para calcular.');const result=calculateConeAngle(values[0]!,values[1]!,values[2]!);setR(result);setError('');void saveHistory({type:'Grados de cono',summary:`Ø${D} / Ø${d} · L${l}`,result:`${roundForDisplay(result.semiAngle)}°`});}catch(e){setError(e instanceof Error?e.message:'Datos inválidos');setR(null)}};return <Screen><ScrollView><SectionHeader title="Grados de cono" subtitle="Calcula el semiángulo y el ángulo total incluido."/><CalculatorCard><NumericInput label="Diámetro mayor (D)" value={D} onChangeText={setD} unit="mm"/><NumericInput label="Diámetro menor (d)" value={d} onChangeText={setd} unit="mm"/><NumericInput label="Longitud (L)" value={l} onChangeText={setL} unit="mm"/>{error&&<Text style={styles.error}>{error}</Text>}<Buttons onCalculate={calc} onClear={()=>{setD('');setd('');setL('');setR(null);setError('')}}/><FormulaCard formula="α = atan((D − d) / (2 × L))"><Text>α es el semiángulo. El ángulo incluido es 2 × α.</Text></FormulaCard></CalculatorCard>{r&&<ResultCard label="Semiángulo" value={`${roundForDisplay(r.semiAngle)}°`} details={<><Text style={styles.resultDetails}>{r.dms.degrees}° {r.dms.minutes}&apos; {roundForDisplay(r.dms.seconds)}&quot;</Text><Text style={styles.resultDetails}>Ángulo incluido: {roundForDisplay(r.includedAngle)}°</Text></>}/>}</ScrollView></Screen>}
